# Diagrams

Export these from your report and drop them here as images (PNG or SVG):

- `erd.png`                 — Entity–Relationship Diagram
- `relational-model.png`    — Relational (normalised) model

Once added, they'll render nicely if you embed them in the main README, e.g.:

```md
![ERD](docs/diagrams/erd.png)
```
