# Orange workflow

Drop your Orange workflow file here:

- `tpe_sales_prediction.ows`

This is the pipeline: File → Impute → Select Columns → (Box Plot / Distributions /
Feature Statistics) → Test & Score (Linear Regression, Random Forest, kNN) → Prediction.

> Open it in Orange, then point the **File** widget at the dataset in `../data/`.
